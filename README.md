# KITT Toolbox

Small Rust utilities shared by KITT products. v0.1 intentionally implements only the resource snapshot needed by the always-on Assistant. Shell execution, patching and filesystem mutation are **not** added until a concrete use case and permission model require them.

```bash
cargo run -- snapshot
```
